{
  "default": {
    "require": ["features/step_definitions/*.ts"],
    "requireModule": ["ts-node/register"],
    "paths": ["recorded/features/**/*.feature"],
    "format": ["progress-bar", "html:recorded/reports/report.html"],
    "timeout": 60000
  }
}
